spring:
  application:
    name: config-server

#spring.profiles.active: native
server.port: 8888
spring.cloud.config.server.git.uri: https://github.com/ignitewala/spring-cloud-webinar-config
     
management:
  endpoints:
    web:
      exposure:
        include: "*"
        